/// An enumeration to define different button types (kept private).
enum EzButtonType {
  /// The default button type with background color.
  regular,

  /// A button type with a border and background color.
  outlined,

  /// A button type with a link color.
  link,
}
